version: '3'

name: integrador

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3002:3000"
    volumes:
      - ./:/home/app
      - ./command.sh:/home/utils/command.sh
  build-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3002:3000"
    volumes:
      - ./:/home/app
      - ./build.command.sh:/home/utils/build.command.sh
